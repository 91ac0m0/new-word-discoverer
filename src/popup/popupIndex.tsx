import ReactDOM from "react-dom/client";
import Page from './popupPage'
import React from 'react';
import "../index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
      <Page />
    </React.StrictMode>
  );